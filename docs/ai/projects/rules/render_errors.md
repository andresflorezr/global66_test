# Render Errors & Debugging

- **UI Errors:** Handle UI rendering errors gracefully.
- **Network Errors:**
  - Catch `DioException` and map them to domain failures.
  - Show user-friendly error messages in the UI.
- **AsyncValue Errors:** Handle `AsyncValue.error` states in the UI.
